class Snake {
    constructor() {
        this.length = 1;
        this.direction = "U"; // U R D L
    }

    turn(dir) {
        console.log(dir);
        switch (dir) {
            case 'U':
                
                break;
            case 'R':

                break;
            case 'D':

                break;
            case 'L':

                break;
            default:
                break;
        }
    }
}

module.exports = Snake;